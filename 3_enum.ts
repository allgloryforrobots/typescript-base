enum Membership {
    Single,
    Standard,
    Premium
}

const membership =  Membership.Standard

// Reversed enum
const reversedMembership  = Membership[2]


enum SocialMedia {
    VK = 'VK',
    FACEBOOK = 'FACEBOOK'
}

const socialMedia = SocialMedia.VK
